<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <table class="table table-bordered">
            <thead>
            <tr>
                <th>Libellé de la formation</th>
                <th class="text-center" style="width: 155px">Priorité</th>
                <th class="text-center" style="width: 100px">CPF</th>
            </tr>
            </thead>
            <tbody>
            {% for formationDemandeeAgent in crep.formationsDemandeesAgent %}
                <tr>
                    <td>{{ formationDemandeeAgent.libelle }}
{#                         ({{ formationDemandeeAgent.formation.code }})#}
                    </td>
                    <td class="text-center">{{ formationDemandeeAgent.priorite }}</td>
                    <td class="text-center">{{ formationDemandeeAgent.dif|ouiNon }}</td>
                </tr>
            {%  endfor %}
            </tbody>
        </table>
    </div>
</div>
